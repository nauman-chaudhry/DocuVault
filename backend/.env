# Environment variables — install dotenv and add require('dotenv').config() at the top of index.js
# NEVER commit this file to git (add .env to .gitignore)

JWT_SECRET=dms-jwt-secret-key-2026
MONGODB_URI=mongodb+srv://admin:admin@cluster0.2bhr1.mongodb.net/docu?retryWrites=true&w=majority&appName=Cluster0
PORT=5000
CLIENT_ORIGIN=http://localhost:3000
